---
title: 日本語のハード折り返しをやめる（設計）
tag: [document]
Project: devops
Task: 008-no-hard-wrap-japanese
created_at: 2026-09-12
updated_at: 2026-09-15
---

# 日本語のハード折り返しをやめる（設計）

## 目的

日本語の md 本文とコメントが桁数で折り返されている。日本語は語の間に空白が無いため、折り返しは語や句の途中で割れる。エディタが soft-wrap するので二重に折れ、意味と無関係な位置で2回切れる。折り返さないことを規約として置き、既存分は機械的に結合する。

## 実測

`~/Joifup` 配下の全リポジトリを測った。「折り返された行」の定義は「次の行が同じ段落の続きである行」（空行でも、見出し・リスト・表・フェンスの開始でもない行が次に来る）。frontmatter とコードフェンスの中は数えていない。

対象は 12 のリポジトリ、md 1,456 ファイル。折り返し率は repo によって 0% から 50% まで開いた。**日本語の折り返し行は合計およそ 21,940 行で、その 98% が 2 つの repo に集中している。** 残りの 10 repo は合わせて 400 行あまりで、うち 3 つは 1% 以下だった。

**0% の repo が実在することが、折り返しが必然ではないことを示している。** 3,036 段落行あって折り返しが 10 行しかない repo があった。また、英語本文が多い repo では折り返し 714 行に対し日本語が 192 行しかなく、日本語に限った規約が対象を自動的に絞る。

この数は着手時点のスナップショットであり、除外の一覧を確定させる前に測っている（いま測り直すと数はわずかに下がる）。**適用の直前には測り直すこと。** 「うち日本語」は行単位で数えた概算である（変換の対象判定は後述のとおり段落単位なので、実際の対象はこれより少し多い）。

## 規約

置き場所は**グローバル `CLAUDE.md` の § 言語**。`~/.claude/CLAUDE.md` は `dotfiles/.claude/CLAUDE.md` への symlink であり、全リポジトリに届く経路はこれだけである。`.claude/rules/` は symlink されておらず dotfiles 内でしか効かないので、全 repo に効かせる規約の置き場所にならない（`tasks/009` が作った `.claude/rules/skill-language.md` は自作スキルの md だけが対象なので、dotfiles 内で閉じていて問題ない）。

**この段落を書いた時点の前提は、その後崩れた。** `tasks/007` が `setup.sh` に `rules` の symlink を足したので、`.claude/rules/` はいまユーザースコープであり、全リポジトリで効く。`skill-language.md` が実際にどこに適用されるかの規定は、そのファイル自身の冒頭にある。

判断の余地がない規則なので、細則ファイルは作らず一文で置く。

> **日本語の本文とコメントは桁数で折り返さない**（語や句の途中で切らない）。エディタが soft-wrap するので二重に折れ、日本語は語の間に空白が無いため語の途中で切れる。**文の切れ目（`。`）での改行は折り返しではないので、そのままでよい**（意味のある位置で切れているため害が無い）。

## 変換の規則

段落の途中で改行されている行を次の行と結合する。

**文の切れ目での改行は折り返しではない。触らない。** 前の行が `。` `！` `？` で終わっているなら（末尾に `**` や `）` のような閉じ記号が付いていてもよい）、そこで結合を止める。

これは当初の規則から直したものである。はじめは「1段落＝1行」として段落の途中の改行をすべて結合していたが、それは規約を広く取りすぎていた。このタスクが問題にしているのは「日本語は語の間に空白が無いため**語や句の途中で割れる**」ことであり、害の根拠も「エディタが soft-wrap するので**意味と無関係な位置で**2回切れる」である。文末は意味のある位置なので、そこで切れていても害が無い。実際、dotfiles に当てたとき旧規則が結合した改行 118 件のうち 31 件（26%）は文末で終わっており、それらは桁数の折り返しではなく「1文1行」という書き方だった。

**`Run:` と `Expected:` の対は、空行を挟んで別の段落にした。** 実装計画はこの対を空行なしの隣接行で書いていたので、markdown では1つの段落であり、描画は元から1行に繋がっていたし、結合の対象にもなった。手順書として読むには2行に分かれていてほしいので、空行を入れて意図どおりの別段落にした（20 箇所を分け、元から2行だった 32 箇所にも空行を入れて揃えた）。折り返しの結合とは別の、markdown の書き方の修正である。**今後 `writing-plans` で計画を書くときは、この対のあいだに空行を置く。**

**触らないもの:**

- frontmatter（ファイル先頭の `---` ブロック）
- コードフェンス（`` ``` `` および `~~~`）の中 —— **フェンスを閉じられるのは、開いたときと同じ文字で、同じ長さ以上のマーカーの行だけである。**それより短いマーカーや別の文字のマーカー、情報文字列（言語名）が付いた行は中身であって、閉じない。閉じられないままファイルが終わる場合は最後まで中身として扱う。当初の実装はマーカーの文字と長さを見ず、マーカーに当たるどの行でも開閉を反転させていた。そのため 4 個のバッククォートで開いたフェンスの中に 3 個の行があると、そこから先のフェンスの内と外が入れ替わり、本来コードである `` ```ts `` ブロックが本文として結合された（TypeScript が 1 行に潰れた）。**この壊れ方は検証の3条件をすべてすり抜ける。** 文字は欠けず、構造の数も変わらず、段落の頭のインデントも変わらないからである。適用先の repo で 16 ファイルの変換結果が変わった。判定は `unwrap.py` と `measure_wraps.py` で同じにし、一致はテストで固定する
    - **バッククォートのフェンスの情報文字列にバッククォートは書けない。** CommonMark の規則であり、含む行はフェンスではなく段落である（`` ```a` の書き方 `` のような行がこれに当たる）。見落とすと、ただの段落行がフェンスを開き、そこから先の内と外が入れ替わって、続く本物のフェンスの中のコードが本文として結合される。**チルダのフェンスにはこの制限が無い**ので、`~~~` には適用しない。
    - **閉じマーカーのインデントは開きマーカーから 3 桁までである。** それより深くインデントされた行は中身であって、閉じない。これも CommonMark の規則である。**絶対の桁数で「3 桁まで」と書いてはならない。** リスト項目の中のフェンスは 4 桁以上インデントされて開き、その閉じマーカーも同じ深さにあるので、絶対で見ると閉じられなくなり、そこから先のファイル全体が中身として扱われる。見るのは**開きからの相対**である。
    - **インデントは桁で数える。タブは次の 4 の倍数の桁まで進む。** CommonMark は行頭の空白を**桁**に展開してから「開きから 3 桁まで」を判定する。`len()` で数えるとタブが 1 桁になり、開きから 4 桁以上の位置にあるタブ付きのマーカーがフェンスを閉じる。そこから先の内と外が入れ替わり、本来コードである領域が本文として結合される。**これはフェンスの入れ子・HTML ブロックに続く 3 件目の、開閉の範囲の取り違えである。** しかも実データで発火していた —— `~/Joifup` 全体の dry-run で唯一 FAILED になっていた joifup の `notes/document/227-joifup-fence-marker-roundtrip.md` の原因がこれであり（同ファイルの `    \t``` ` の行）、**第1〜4条件はすべて素通りし、第5条件（AST 比較）だけが捕まえた**。判定は `unwrap.py` と `measure_wraps.py` の両方を直し、一致をテストで固定した。
- 表の行（`|` で始まる）
- **HTML ブロック（`<!--` のコメントと、行頭がタグの領域）の中** —— markdown では、`<!--` から `-->` までのコメントや、ブロックレベルのタグで始まる領域は HTML ブロックであり、**中身はそのまま出力される**（markdown として解釈されない）。ここを結合すると、閉じの `-->` が箇条書きの項目にくっつき、`<div>` のようなタグでは構造そのものが壊れる。**この壊れ方はフェンスの欠陥と同じ種類で、検証の3条件をすべてすり抜ける** —— 文字は欠けず、構造の数も変わらず、段落の頭のインデントも変わらないからである。実際、joifup-blog の記事テンプレートで `<!--` の次の行が開始行に潰れ、閉じの `-->` が最後の箇条書きの項目の末尾にくっついた（コメントなので描画は変わらず、目で見ても分からない）。どこまでを HTML ブロックとみなすかは次のとおり。
    - **コメント**: 行頭（インデントは 3 桁まで）が `<!--` である行から、`-->` を含む行まで。その行も含む。閉じが無ければファイルの終わりまで中身として扱う（フェンスと同じ）。**`<!-- 一行のコメント -->` は 1 行で閉じる**ので、開始行そのものも閉じの判定にかける。フェンスは開きマーカーの行が自分を閉じることはないので、この点だけ扱いが違う。**行の途中にある `<!--` は開かない。** インラインのコメントは段落の一部だからである。
    - **`<pre>` `<script>` `<style>` `<textarea>`（CommonMark の 1 種目）**: 行頭（インデントは 3 桁まで）がこの 4 つのいずれかのタグで始まる行から、**閉じタグ（`</pre>` `</script>` `</style>` `</textarea>` のいずれか）を含む行まで**。**この 4 つだけは空行では終わらない。** 他の種別と同じ「空行まで」で扱うと、空行をまたいだ続きが本文として結合される。`<pre>` は空白が意味を持つので、結合すれば描画が変わる。実データには 0 件だが、**フェンスの欠陥と完全に同じ種類（開閉の範囲の取り違え）なので、件数に関わらず塞ぐ。** 閉じが無ければファイルの終わりまで中身として扱う（フェンスと同じ）。終了条件が 4 つの閉じタグのどれでもよいのは CommonMark の規定どおりで、開いたタグ名と一致している必要はない。大文字小文字は区別しない。判定の順序は下の「タグ」より先である（`<pre>` はタグの形にも当たるため、逆にすると空行で終わる扱いに戻る）。
    - **タグ**: 行頭（インデントは 3 桁まで）が `<` または `</` で始まり、ASCII の英字で始まるタグ名が続き、そのあとが空白・`>`・`/>`・行末のいずれかである行から、**空行まで**。CommonMark の HTML ブロック 7 種類をすべて実装してはいない。6 種目（既知のブロック要素名の一覧）より**広く**取り、`<span>` や `<ErrorPanel>` のような名前も含める。**迷ったら触らない側に倒す** —— 結合しそこねるのは折り返しが残るだけだが、HTML を壊すのは戻せないからである。`<https://example.com>`（自動リンク）や `<型引数>` はタグ名の形に当たらないので開かない。
    - **フェンスの中の `<!--` や `<div>` は HTML ブロックではない**（フェンスの中身である）。判定の順序はフェンスが先である。
    - 判定は `unwrap.py` と `measure_wraps.py` で同じにし、一致はテストで固定する。HTML ブロックの開始行は段落を切る（段落の続きに見える位置に来ても、そこで止めて触らない）。
- **コンテナディレクティブの行（行頭がコロン 3 個以上）** —— `::::columns` / `:::column` / 閉じの `:::` のような行である。適用先のリポジトリがプロダクトの記法としてこれを使っている（実データに 9 行）。**行頭（インデントは 3 桁まで）がコロン 3 個以上で始まる行は本文の行ではない**ので、単独で触らない行として扱う。現時点で壊れていないのは、たまたま `:::column` の直後が見出しで段落が切れているからにすぎず、**日本語の段落の直後に閉じの `:::` が来た瞬間に本文として結合される**（`:::column 左のカラムの本文が折り返されている ::: ::::` になり、検証は「安全」と誤判定する）。
    - **フェンスのように開きと閉じを対応させることはしない。** 中身は普通の markdown の本文（見出し・段落・フェンスを混在できる）であって、結合してよいからである。フェンスと同じ「開いたら閉じるまで中身」にすると、カラムの中の折り返しが一切直らなくなる。必要なのは「マーカーの行を本文に巻き込まない」ことだけで、それはマーカー行を単独の触らない行にすれば足りる。開閉の対応を持たない分、閉じ忘れや入れ子の深さを取り違えて**そこから先のファイル全体の内と外が入れ替わる**壊れ方も起きない。対応させないので、開きの情報文字列（`:::column`）と閉じ（`:::`）を区別する必要もない。
- 見出し（`#`）、引用（`>`）、水平線
- 半角空白4つ以上またはタブで始まる行 —— markdown ではこれもコードブロックである
- **意図的なハードブレイク（行末が半角空白2つ、または `\`）を含む段落** —— 段落ごと触らない。ハードブレイクの前後だけ結合を止める形にすると規則が込み入るわりに、実数は joifup の notes 全体で15件しかない。**判定は行末の `\r` を許して読む。** 見ないと `本文  \r` に一致せず、CRLF の入力では保護が**黙って外れる**（結合された結果は `_squeeze` も markdown-it も `\r` を吸収するので、検証の 5 条件すべてを通る）。実データに CRLF も BOM も 0 件だが、扱いは `<pre>` と同じ基準である —— **黙って通る同種の欠陥は件数に関わらず塞ぐ**
- 日本語を含まない段落 —— 英語は空白で切れるので害が小さく、規約も日本語に限っている。**判定は行単位ではなく段落単位で行う。** 行単位にすると、英語の行で始まって日本語の行が続く段落が途中までしか結合されず、かえって読みにくい形で残る

**結合するもの:** 上記に当たらない段落本文の行と、箇条書き（`-` / `1.`）の項目本文が複数行に折られている場合の継続行。継続行は親の行に結合する。

**行頭のインデントは必ず戻す。** 箇条書きの項目の中には、空行を挟んでインデントされた段落が続くことがある。結合のために行を `strip()` したあとインデントを戻し忘れると、その段落が桁 0 に出てリストがそこで切れる。実装で実際に3件起きた。**段落テキストを比べる条件も構造の数を数える条件もこれを素通りする** —— リスト項目の数は変わらず、段落テキストの比較は空白を無視するからである。捕まえるのは段落の頭のインデントの並びを見る条件のほうで、`## 検証` の3つ目がまさにこのために在る。それでも検証に頼らず、結合の側でインデントを保つ。

**結合点の空白:** 結合点の**前後がともに日本語**（かな・漢字・全角の記号）なら空白なしで繋ぎ、**それ以外は半角空白1つ**を入れる。ただし全角の約物には例外を置く。**左が全角の句読点（`。` `、`）なら、右が何であっても空白を入れない。** **右が全角の開き括弧（`（` `「` `『` `【` `〔`）なら、左が何であっても空白を入れない。** 日本語では句読点の直後にも開き括弧の直前にも空白を置かないからである。

**閉じ括弧の側（`）` `」` `』` `】` `〕` が左）には例外を置かない。** 上と同じ数え方で `）` の直後に半角の可視文字が来る形を数えると、空白なしが 2,430 件・空白ありが 2,172 件（53:47）で、開き括弧の側の 93:7 のような支配的な慣習が無い。どちらに倒しても既存の書き方のおよそ半分に反することになるので、例外を置かず一般の規則（両側が日本語なら空白なし、それ以外は半角空白1つ）に委ねる。

どちらも既存の書き方から決めた。**数え方は次のとおりである。** 対象は折り返しが集中している 2 つの repo の *.md 926 ファイル（`unwrap.py` の除外一覧を当て、frontmatter とコードフェンスの中は数えない）。位置は行の内側（改行をまたがない位置）に限る。ここで「ascii」は**日本語でない文字**、すなわち `unwrap.py` の `JA`（かな・漢字・全角の約物）に当たらない文字を指す。ASCII の記号もバッククォートも含む。結合点の判定が「両側が日本語か」で書かれている以上、数えるべきもそれと同じ切り方である。この数え方で `（` の直前を見ると、空白なしが 12,470 件、空白ありが 920 件で 93% が空白なしである。ASCII 英数（`[A-Za-z0-9]`）に狭めると 4,314 件 対 2 件になる。どちらの切り方でも、開き括弧の直前に空白を置かないのが支配的である。例外を置かないと、`実体パスへ解決される。 ` ＋ `` `brew upgrade tmux` `` のような箇所と、`` `.claude` `` ＋ `（対象は…` のような箇所に空白が入る。dotfiles では前者が 18 箇所あった。後者は、例外を片側だけにしていた時点で、その 2 つの repo に当てると `（` に 531 件・`「` に 65 件の空白が新しく入る計算だった（同じ対象・同じ除外で、右側の例外を外したときに空白が入る結合点を数えた値。`「` は当初 55 件と書いていたが片方の repo の分しか数えていなかった）。

判定を「どちらかが ASCII 英数か」で書くと規則が変わってしまう。記号どうし（`:` と `` ` ``、`` ` `` と `—`）が結合点に来たときに空白が落ち、`使う:` と `` `brainstorming` `` が `使う:`brainstorming`` のようにくっつく。実装で実際に起きた。見るべきは「両側が日本語か」であって「片側が英数か」ではない。

折り返しで消えた空白は復元できないので、既存の本文の慣習に寄せて決めた。行の内側（改行をまたがない位置）で英数字と日本語の境界に空白が入っている割合を主な 3 つの repo の notes で数えると、日本語 → 英数字が 38,190 / 68,846（55%）、英数字 → 日本語が 60,260 / 85,315（71%）で、どちらも空白を入れる側が多数派である。`Claude Code` のように英数字の語句が行をまたいで割れた場合に語が潰れるのは、この規則でも防げる。

## 直り切らないものを見えるようにする

**4スペース以上のインデント行は触らない。** markdown では、その行がインデントされたコードブロックなのか、箇条書きの項目の継続行なのかを一般に区別できない。コードブロックを結合すると壊れるので、触らない側に倒す。結果として、継続行が4スペース以上で書かれた入れ子の箇条書きは折り返しが残る。

残ったものを黙って残さないために、**適用したあとに折り返しの残量を測って報告する。** 変換スクリプトが「変更なし」と答えることと、そのファイルに折り返しが残っていないことは別である。前者だけを見て終わりにすると、直り切らなかった分が「直った」ものとして扱われる。

次の3つは、実装の途中でレビューが「壊れる入力がある」として挙げたものである。いずれも実データを当たると `~/Joifup` 配下に1件も無かったが、扱いは分けた。

- **先頭が `|` でない GFM 表**（`列 | 値` のヘッダに `---|---` が続く形）は表と認識されず、結合されて壊れる。しかも壊れ方が検証の2条件のどちらにも引っかからず、安全と誤判定される。**検証がすり抜けること自体が問題なので、表の区切り行を構造として数えるようにした。** 結合で区切り行が消えると数が変わり、そのファイルは失敗として書き戻されない。
- **Setext 見出しの下線**は見出しの境界と認識されず、前の行と結合される。`===`（h1）だけでなく、**`-` や `--`（ハイフン 3 個未満）も正当な Setext の h2 下線**である。ハイフン 3 個以上は水平線（`RULE` の `-{3,}`）に当たるので結合されないが、`-` と `--` は当たらないので `===` と同じく結合される。どちらも実データ 0 件なので対処しない。将来書き始めたときのために、壊れることだけ記録しておく。**ただし、これらはいずれも検証の第4条件（下記）で捕まる** —— 英数字も CJK も含まない非空行だからである。壊れたまま書き戻されることはなく、失敗として報告される。
- **定義リスト**（`用語` の行の次に `: 定義` が続く形）は結合され、**検証の 5 条件すべてを通る**。CommonMark に無い構文なのでパーサ（第5条件）からはただの 1 段落に見え、`:` 1 個は `:::` の判定（第4条件）に当たらず、CJK を含むので第4条件の「マーカーだけの行」にもならない。実データ 0 件なので対処しない。**壊れるうえに検証も捕まえられないものとして、ここに記録しておく。**

**joifup の `227-joifup-fence-marker-roundtrip.md` は直った。** タブ展開を入れる前は `~/Joifup` 全体の dry-run で唯一の FAILED であり（第5条件が「39 番目のブロック (fence) の中身が変わった」を返していた）、直したあとは検証が `[]` を返して markdown-it と一致する。`~/Joifup` 全体の md 1,691 ファイルに当て直しても、結果が変わるのはこの 1 ファイルだけである（FAILED → 変換できる、に変わる）。

**HTML ブロックと誤認された領域の折り返しも、残ったことが数に出ない。** HTML ブロックの判定は「迷ったら触らない側に倒す」として CommonMark より**広く**取ってある。広く取った分だけ本文が巻き込まれることがあり、巻き込まれた領域の折り返しは変換されないまま残る。ところが残量の測定も同じ判定で HTML ブロックの中を数えないので、**残ったことが数にも出ない。** 引用（`>`）と同じ種類の盲点が、`:::` の追加とあわせてもう一つ増えたことになる。広い判定は安全側だが、そのぶん残量測定を盲目にする —— 安全側に倒すことの代償はここに出る。

**検証の 5 条件をすべて通り抜けて壊れるものが、いまも 3 つある。** 実データにはどれも 0 件だが、記録しておく。

- **定義リスト**（`用語` の次行に `: 定義`）—— 結合される。CommonMark に無い構文なので AST 比較が盲で、日本語を含むので第4条件も盲である
- **CRLF の行末の混在** —— 結合された段落行だけが `\r` を失い、そのファイルの中で行末が混ざる。ハードブレイクの保護は塞いだが、こちらは残っている
- **`  \t` で字下げされたコードブロック** —— `INDENTED_CODE` はタブを展開しないので本文として結合する。ただし**これは第5条件が捕まえる**ので、黙っては通らない（失敗として報告され、書き戻されない）

**引用（`>`）の中の折り返しも残る。** 引用行は触らないので、引用の中で文の途中や強調の途中に改行があってもそのままである。しかも残量の測定でも引用行は数えないので、**残ったことが数にも出ない。** dotfiles では2箇所だが、引用を多く使う repo ではもっと残る。

## 検証

21,940 行を目視せずに安全だと言える根拠を、スクリプト側に持たせる。ファイルごとに変換前後を比べ、次の5つがすべて成り立つときだけ書き戻す。

1. **段落テキストが文字単位で一致する。** 変換後から改行と結合時に挿入した空白を除いたものが、変換前から改行を除いたものと一致する。文字の欠落・重複・順序変更が無いことを保証する。
2. **構造の数が不変である。** 見出し・コードフェンス・リスト項目・表行・表の区切り行の数が前後で変わらない。
3. **段落の頭のインデントの並びが不変である。** 段落の先頭行（空行の次の非空行）の行頭インデント幅を順に並べたものが、前後で一致する。
4. **マーカーだけの行が不変である。** 英数字も CJK（かな・漢字）も含まない非空行は、変換後も同じ内容の独立した行として、同じ数だけ同じ順で存在する。
5. **独立したパーサで見たブロックの列が不変である。** `markdown-it-py` で変換前後をパースし、ブロックの列（種別・ネストの深さ・ブロックごとの中身）が一致する。

どれかが崩れたファイルは変換せず、失敗として報告する。

3つ目は、**インデントを落としてリストを切る壊し方が1つ目も2つ目も素通りする**から要る。リスト項目の数は変わらず、段落テキストの比較は空白を無視するので、どちらも気づかない。実際に一度これで3箇所を壊した。結合の側でインデントを保つ実装にして直したが、検証にも同じことを見させておく。見るのは段落の先頭行だけなので、結合されずに複数行のまま残った段落の2行目以降のインデントは見ていない。

4つ目は、**ブロックの境界を本文に巻き込んだ**ことを見る。1〜3 は「文字列としての md」の性質しか見ておらず、**ブロックの境界がどこにあるか**という情報を一切参照しない。ところがこの道具がこれまで出した欠陥は 2 つともまさにその情報を壊すもので（フェンスの入れ子と HTML ブロック）、3条件をすべてすり抜けた。4つ目は `unwrap` のブロックモデルに一切依存しない**純粋に語彙的な**不変量なので、モデルの側の取りこぼしに巻き込まれない。`-->`・`:::`・`$$`・Setext の `===`・TOML の `+++` はどれも本文に巻き込まれた瞬間に独立した行でなくなるので、数と並びが変わる。実測では、**今回の HTML コメントの欠陥（修正前の版）を検出でき**、誤検出は `~/Joifup` の md 1,691 ファイル・変換対象 690 ファイルに対して 0 件だった。

**4つ目でも捕まらないものがある。** フェンスの欠陥（`` ``` `` の入れ子）と HTML ブロックの 1 種目（`<pre>`）の欠陥は、**この条件をすり抜ける** —— どちらもマーカーの行そのものは独立した行として全部残り、壊れるのはマーカーに挟まれた中身のほうだからである。それは承知のうえで入れている。4つ目は 3 条件の穴を**部分的に**塞ぐものであって、ブロックの認識が正しいことの代わりにはならない。

### 5つ目: 独立したパーサによる AST 比較

**4つ目が捕まえられない「マーカーに挟まれた中身」を、5つ目が捕まえる。** 1〜4 はブロックの境界を一切参照しない。かといって境界を `unwrap` 自身の分類器（`fence_open`・`html_block_open`）で測れば自己参照になり、**分類器のバグは定義上検出できない** —— そしてこの道具が出した欠陥 2 件はどちらも分類器のバグだった。第二の観測者が要る。

`markdown-it-py` を**任意の依存**として入れる。変換そのものは依存ゼロのまま保ち、使うのは「変換が壊していないか」を確かめる側だけである。パーサが入っていない環境ではこの条件だけが働かず、**そのことをスクリプトが警告として出す**（黙って飛ばさない。検証が 1 つ欠けたまま「成功」に見えるのが、この道具で最も危ない形である）。

**何を正規化し、何をしないか。**

- 段落・見出し・リスト項目の本文（`inline`）は**空白と改行をすべて落として**比べる。段落の中の改行を消すことこそがこの道具の仕事なので、そこを差と見なしてはならない。結合点に入れる半角空白も同じ理由で落とす。
- **正規化はブロックの中だけに閉じる。** 既存の第1条件は文書全体で空白を潰すので、文がどのブロックに属するかが変わっても見えない。中身をブロックごとに区切って持つことが、第1条件との決定的な差である。
- **フェンス・インデントされたコードブロック・HTML ブロックの中身は一切正規化しない。** `unwrap` はそこを 1 文字も触らないのだから、1 文字でも違えば欠陥である。ここを他と同じように空白ごと潰すと、**フェンスの欠陥がそのまま素通りする** —— `consta=1;constb=2;` は前後で同じだからである。実際に出た欠陥を捕まえられない正規化は、正規化してはならない側に入っている。

**実証。** 修正前のフェンス処理（マーカーの文字と長さを見ず、`` ``` `` に当たるどの行でも開閉を反転させていた版）に戻したコピーで、4 個のバッククォートのフェンスに囲まれた ```` ```ts ```` ブロックを変換した。中身のコードが本文として結合され、**第1〜4条件はすべて素通りした**。5つ目だけが「1 番目のブロック（fence）の中身が変わった」を返した。**実データでも同じことが起きた** —— フェンスのインデントのタブを展開していなかった欠陥（3 件目の、開閉の範囲の取り違え）は、`~/Joifup` 全体で 1 ファイルだけ発火し、5つ目だけが捕まえた。

**5つの条件が `verify()` に配線されていること自体を、テストで固定する。** 条件ごとの検査を書いても、`verify()` から呼ぶ行が消えれば検証は黙って 1 つ欠ける —— この道具で最も危ない形である。実際、5つの配線を 1 行ずつ外す変異を当てたところ、**5 つのうち 4 つが 139 件のテストを全部すり抜けた**（捕まったのは第4条件だけ）。条件ごとのテストの多くが検査関数を直接呼んでいて、`verify()` を通っていなかったためである。いまは条件ごとに「その条件だけが当たる入力」で `verify()` を呼び、返るリストを完全一致で突き合わせる。どれか 1 つの配線が外れれば、その 1 つのテストだけが落ちる。

**5つ目でも残る穴。** パーサは CommonMark しか知らないので、**CommonMark にない構文の境界は見えない** —— コンテナディレクティブ（`:::`）・数式（`$$`）・frontmatter は、パーサから見ればただの段落や水平線である。そこを本文に巻き込む壊し方は 4つ目（語彙的なマーカー行）だけが見る。逆に 4つ目が見られない「マーカーに挟まれた中身」は 5つ目が見る。**2 つは互いの穴を埋め合う関係にあり、どちらも落とせない。**

**そして 5 条件すべてを通っても、「本文でないものを本文として結合した」形が残る。** 結合の結果が CommonMark としては前後で同じ構造になる場合である（実例: 著者が ```` ```markdown ```` フェンスの中に `` ``` `` の木構造図を書いたファイル。CommonMark では内側の `` ``` `` がフェンスを閉じるので、木構造図は本文であり、両方のパーサがそう読む。結合は規格どおりに正しく、見た目だけが壊れる）。これを塞ぐ道は無いので、**人間に見せる**。

### 検証を通ったあとに人間へ見せるもの

新しく生まれた結合点のうち、**左右のどちらかが日本語でも英数字でもない**ものを一覧で出す。適用時（`--apply`）と dry-run の両方で出す。

今回の HTML ブロックの欠陥なら `-->` が、フェンスの欠陥なら `1;` が、コンテナディレクティブなら `:::` が、この一覧に出る。`/` はノイズを落とすために普通の文字として扱っているので、`//` は出ない（パス表記の行末が全体の3割を占め、信号を埋めていた）。**「検証は通ったが見たことのない形が出た」を報告する枠が、これまで完全に欠けていた。**

**読める量に収める工夫が要る。** 素朴に出すと読まれない。

- **同じ形をまとめて数える。** `-->` が 20 件あれば「`-->` との結合が 20 件」の 1 行にする。多い順に並べ、上位 20 形だけを出して残りは形の数だけ添える。新しく生まれた形は普通ひとかたまりで現れるので（`-->` の欠陥は全ファイルに 1 件ずつ散らばった）、多い順が効く。
- **インラインの markdown 記法を結合点から剥がす。** 強調（`**`）・コードスパン（`` ` ``）・リンク（`[]()`）は普通の本文の途中に普通に現れる。剥がさずに実測すると fde で 2,101 件・1,600 形が出て、先頭は `` `document/010-…` `` のようなただのコードスパンで埋まった。`<` と `>` は剥がさない（HTML のタグはまさに見たい形であり、自動リンクが数件出るのは許容できる損である）。
- **日本語の技術文書の本文に普通に出る記号を「普通の文字」に足す。** `——`・`→`・`①`・`§`・`°`・`×`、そして **`/` と `.`** である。`——` だけで 181 件あり、足さないと一覧の先頭が本文の飾りで埋まる。**`/` と `.` はノイズの最上位だった。** `~/Joifup` 全体で 999 件 / 495 形のうち `/` が 317 件（32%）を占め、実体は `` `a` / `b` / `c` `` のような列挙の行末で全部本文である。足すと 605 件 / 433 形に落ちる。`.` はさらに 88 件 / 71 形を落とし、消えるのは `` `.gitignore` ``・`` `.uat-evidence/<task>/` `` のような**記号で始まるコードスパン**と英文の終止符だけで（全件を目で確かめた）、ブロックのマーカーは 1 件も含まれない。**足してもブロックの境界は見える** —— `-->` は右が `-`、`</div>` は右が `<`、`:::` は `:`、`$$` は `$`、`===` は `=`、`+++` は `+` で当たり続ける。`.` は剥がす側（インラインの markdown 記法）ではなくこちらに足す。`.` は markdown の記法ではないので、剥がす側に置くと名前が意味と合わないうえ、実測でも落ちる形はこちらのほうが広い。**罫線（`│` `├──`）は足さない** —— フェンスの外の木構造図が結合されている印であり、まさに見たい形だからである。この判定は結合点の空白を決める規則（`JA`）とは別に持つ。`JA` を広げると出力が変わるが、こちらは何を見せるかを決めるだけである。

**実測（この 3 つを入れたあと）。** fde の worktree で 167 ファイル変換・131 件 / 84 形、joifup で 481 ファイル変換・853 件 / 409 形。どちらも一覧の出力は見出しを含めて 23 行に収まる。dotfiles は変換済みなので 0 件である。

**この一覧は実際に働いた。** fde の `notes/plan/009` で、著者が ```` ```markdown ```` フェンスの中に書いた木構造図が本文として結合されているのを、`├──` との結合 4 件・`│` との結合 4 件として先頭付近に出した。5 条件はすべて通っている（CommonMark としては結合が正しいため）。一覧が無ければ誰も気づかなかった。

**段落の境界の判定は `unwrap.py` と `measure_wraps.py` で同じにし、一致はテストで固定する**（除外の一覧・文末・フェンス・HTML ブロックと同じ方針）。両者には不一致があった。`measure_wraps.py` の `BLOCK_START` は `===` を段落の境界として扱うが `unwrap.py` は扱わず、逆に `unwrap.py` の `RULE` は `***` と `___` を水平線として扱うが `BLOCK_START` は扱っていなかった。**`unwrap.py` 側に揃えた** —— 測るのは「変換したあとに何が残っているか」なので、変換する側の見え方に合わせるのが正しい。`===` を measure 側だけが境界として扱うと、`unwrap` が結合してしまう改行が残量に出ず、直っていないものが「直った」ことになる。

スクリプトには「触らないもの」の項目ごとに単体テストを付ける。フェンス内・表・ハードブレイク・frontmatter・英語段落がそれぞれ保存されること、日英境界に空白が入ること、リスト継続行が結合されることを固定する。

## 適用の範囲と順序

**この作業（tasks/008）で行うのは、規約の追加と dotfiles 自身の変換、そして変換スクリプトの確定まで。**

**スクリプトは `.claude/scripts/` にコミットする。** 当初は「一度きりの機械的変換だから恒久的な道具を持たない」として scratchpad に置く方針で始めたが、適用先が dotfiles だけでなく他の repo にも及ぶことが決まった時点で前提が崩れた。道具は複数回使われる。

スクリプトを残さない場合、リポジトリに残る唯一の正典は実装計画に埋め込まれたコードになる。**実際にそれが古いまま取り残された。** 実装の途中で規則が3回変わった（ハードブレイクの扱い、インデントの復元、結合点の空白）のに、計画のコードはその前の版のままだった。そのまま次の repo に当てれば、いま直した欠陥をそっくり再現する。とくに、実装中に入れた「除外は対象ディレクトリからの相対で判定する」という修正はどこにも記録されていなかった。これが無いと worktree の中で走らせた瞬間に全ファイルが除外され、**「変更なし・失敗 0」と出て成功に見える。**

**「変更なし・失敗 0」に見える形は他にもある。** 対象ディレクトリを渡し忘れると、何も走っていないのに同じ行が出て exit 0 していた。引数が空なら使い方を表示して exit 2 する。**打ち間違いも同じ形になる。** 存在しないディレクトリを渡したとき、走る対象が 0 件でも同じ行が出て exit 0 していた。対象がディレクトリとして存在しなければ、その旨を出して exit 2 する。`measure_wraps.py` も同じ扱いにする。除外には `node_modules` などに加えて `.superpowers` と、worktree の置き場所である `worktrees` `.worktrees` の両方を入れてある。repo ルートを渡したときに SDD の作業ファイルまで書き換えないためである。**除外はパスの接頭辞ではなく、対象ディレクトリからの相対パスの要素名で判定する。** 接頭辞で持つと、同じ除外が対象の渡し方によって効いたり効かなかったりする。`"/.claude/worktrees/"` を接頭辞で持ったまま対象を `<repo>/.claude` にすると、相対パスが `/worktrees/...` になって一致せず、**進行中の別ブランチの worktree を書き換える。** 実際、main の dotfiles で `unwrap.py .claude` を dry-run すると 95 件のうち 86 件が対象になり、うち 70 件が worktree の中のファイルだった。しかも実装計画が案内していたのがまさに `<WT>/.claude` の形である。要素名で判定すれば、対象をどこに置いても、また対象の内側でも外側でも同じように効く。除外の一覧は `unwrap.py` と `measure_wraps.py` で同じにし、**一致はテストで固定する**。片方だけが数えると、変換しないと決めた場所の折り返しが残量に出て、直し切れない数がいつまでも残る。読めないファイルが1件あっても全体を落とさず、そのファイルを失敗として報告して続ける。他の repo には壊れたファイルがありうる。

テストが付いているので、次に使う人はまずテストを走らせて、道具が壊れていないことを確かめられる。これが埋め込みコードには無い。

置き場所は `.claude/skills/<skill>/scripts/` ではなく `.claude/scripts/`。これはワークフローの入口ではなく、移行のための道具だからである。適用が全 repo で終われば消してよい。

他の repo は同じ手順を repo ごとに別 PR で順次適用する。適用時の状態はそれぞれ違う:

- **最大の repo**（13,571 行）: この設計を書いた時点では main に未コミット変更が5ファイルあった。先に片づけてから当てる。下と同じく、**状況と数は当てる直前に確かめ直す。**
- **2 番目の repo**: 進行中のブランチがあるあいだは当てない。先に main を変換すると、そのブランチが触っている notes とマージ時に全面衝突する。**当てる直前に `git worktree list` と各ブランチの状態を見て、窓が開いていることを確かめること。**（この設計を書いた時点では `feature-049` が In review で `notes/document` を14ファイル触っていた。その後 049 はマージされ、いまは `feature-050` が進行中である。数も動く —— 当初 7,963 行と測った日本語の折り返しは、その後の作業で 8,376 行に増えた。**ここの数字と状況は当てる直前に測り直す。**）
- **残りの 6 repo**（合計 287 行）: 小規模なのでまとめて順次。
- **`~/Joifup/notes` と `~/Joifup/tasks`**（52 行）: git 管理外なので PR は無く、直接変換する。

コード側のコメントの折り返しは本タスクの対象外である。先に整形すると、後続のタスクが書き換える行を二重に触ることになるからである。

**この節を書いた時点では `tasks/007` が対象のコメントブロックをどのみち書き換えるので済むと見込んでいたが、そうはならなかった。** 007 は触った行だけを直し、残りの整形は「触る機会に合わせて直す」として先送りしている（`notes/document/007-stop-duplicating-rationale-design.md` の `### 折り返しの規約` が正典）。したがって、まだ桁数で折り返されたまま残っている docstring がある。
